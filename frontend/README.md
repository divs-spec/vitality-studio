# Frontend (Electron + React)

To run frontend dev server:

```bash
cd frontend
npm install
npm run dev
```

To run Electron pointing at the built files:

```bash
cd frontend
npm install
npm run build
npm run start
```
